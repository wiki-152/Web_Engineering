const person = {
    name: "John",
    age: 30
};

const printAge = function (person) { 
    console.log(person['age']); 
}
printAge(person); 


// The age should be in '' not in [] and  => is not used syntax was wrong